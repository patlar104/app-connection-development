package dev.appconnect.data.local.database

import androidx.room.TypeConverter

class Converters {
    // Add type converters if needed for complex types
    // For now, all our types are basic (String, Long, Boolean)
}

